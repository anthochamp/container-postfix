name: postfix container pre-publish

on:
  workflow_call:
    inputs:
      checkout-ref:
        description: The branch, tag or SHA to checkout. Defaults to the reference or SHA for that event.
        required: false
        type: string
      dockerfile-dir:
        description: Dockerfile directory
        required: false
        type: string
    outputs:
      container-image-name:
        value: ${{ jobs.postfix-pre-publish.outputs.container-image-name }}
      postfix-version:
        value: ${{ jobs.postfix-pre-publish.outputs.postfix-version }}

jobs:
  postfix-pre-publish:
    runs-on: ubuntu-latest
    outputs:
      container-image-name: ${{ steps.container-image-name.outputs.VALUE }}
      postfix-version: ${{ steps.postfix-version.outputs.VERSION }}
    steps:
      - name: Compose container image name
        id: container-image-name
        run: echo "VALUE=${REPO_NAME#container-}" >>$GITHUB_OUTPUT
        env:
          REPO_NAME: ${{ github.event.repository.name }}

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.checkout-ref }}

      - name: Find out embedded postfix version
        id: postfix-version
        run: echo "VERSION=$(scripts/postfix-version.sh "${{ inputs.dockerfile-dir }}")" >>$GITHUB_OUTPUT
